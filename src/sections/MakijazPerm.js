import React from "react";
import SectionBuild from "./SectionBuild";
import { permMakeupData } from "../data/permMakeupData";

const MakijazPerm = () => <SectionBuild sectionData={permMakeupData} />;

export default MakijazPerm;
